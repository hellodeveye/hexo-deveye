<!--<% if (site.posts.length){ %>
  <div class="widget-wrap">
    <h3 class="widget-title"><%= __('archive_a') %></h3>
    <div class="widget">
      <%- list_archives({show_count: theme.show_count, type: theme.archive_type}) %>
    </div>
  </div>
<% } %>-->

<% if (site.categories.length){ %>
<section class="widget widget_posts_entries">
	<h4 class="widget-title"><%= __('archive_a') %></h4>
	<%- list_archives({
		show_count: false, 
		type: theme.archive_type,
		format: 'YYYY年MM月'
	}) %>
</section>
<% } %>